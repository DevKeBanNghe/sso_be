export enum TokenExpireIn {
  ACCESS_TOKEN_EXPIRE_IN = '3h',
  REFRESH_TOKEN_EXPIRE_IN = '1d',
}

export enum TokenExpireIn {
  COOKIE_ACCESS_TOKEN_EXPIRE_IN = '4h',
  COOKIE_REFRESH_TOKEN_EXPIRE_IN = '2d',
}

export const KEY_FROM_DECODED_TOKEN = '_client';
